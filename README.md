# hash

Capability-free cryptographic hash primitives in canonical `.kotoba`.

The initial implementation provides SHA-1 for interoperability protocols such
as BitTorrent v1. SHA-1 is not exposed as a modern signature or password hash.
